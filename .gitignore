venv/
.venv/
__pycache__/
*.pyc
*.log
sprint-recap.json

# Working-folder pptx outputs are not committed; the test fixture is the
# one exception — it is generated by tests/fixtures/_build_template.py and
# committed to keep the test suite reproducible.
*.pptx
!tests/fixtures/*.pptx
